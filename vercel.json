{
    "version": 2,
    "buildCommand": "npm run build",
    "devCommand": "npm start",
    "builds": [
        {
            "src": "./src/index.ts",
            "use": "@vercel/node"
        }
    ]
}
